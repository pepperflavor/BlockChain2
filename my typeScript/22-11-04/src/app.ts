class Department {
    name: string;

    constructor(n: string){
        this.name = n; // Department객체가생성될때 name의 값을 n에서 얻는다
    }
}

const accounting = new Department('Accounting');
console.log(accounting);

//# sourceMAppingURL=app.js.map


